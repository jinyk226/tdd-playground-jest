{
  "name": "tdd-playground-jest",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "jest --watchAll --verbose"
  },
  "devDependencies": {
    "vite": "^2.9.5"
  },
  "dependencies": {
    "@types/jest": "^27.4.1",
    "jest": "^28.0.1"
  }
}
